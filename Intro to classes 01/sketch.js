
const fruits = [23, 53, 19, 32];

function setup() {
  createCanvas(500, 500);
  background(200);
}

function draw() {

}

class BarChart {
  // constructor defines everything that's in the object
  constructor () {
    // will give each instance of the class a heigth property of 300
    this.height = 300;
  }
}

// creates object from BarChart class
let chart1 = new BarChart();